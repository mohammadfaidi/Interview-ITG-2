- GET requests is only used to request data.
- GET requests can be cached.
- GET requests remain in the browser history.
- GET requests can be bookmarked.
- GET requests should never be used when dealing with sensitive data (Less Secure Than Post).
- GET requests have length restrictions.
- GET variables are displayed in the URL.


- POST is used to send data to a server.
- POST requests are never cached.
- POST requests do not remain in the browser history.
- POST requests cannot be bookmarked.
- POST requests have no restrictions on data length.
- POST variables are not displayed in the URL.